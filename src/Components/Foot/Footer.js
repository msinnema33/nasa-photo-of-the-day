import React from 'react';



const FooterInfo = props => {
    return(
    <div>
        <h4 className = "footer">{data.copyright} </h4>  //need to add curly braces around props.title  
        <p> Copyright 2020</p>
    </div>
);

    };

export default FooterInfo;